<div class="pet-index-container">
  <div class="row">
    <div class="col-10">
      <h1><%= @pet.name %>'s History</h1>
    </div>
    <div class="col-2">
      <%= link_to new_user_pet_log_path(@user, @pet) do %>
        <i class="fa-solid fa-plus" style="font-size: 30px"></i>
      <% end %>
    </div>
  </div>
  <div id="loading-overlay">
    <div class="loader"></div>
    <p style="margin-top: 15px; font-weight: bold;">Gemini is analyzing your data...</p>
  </div>
  <div class="logs-all justify-content-center">
    <% if @logs.any? %>
      <% @logs.each do |log| %>
        <div class="card-logs mb-2 p-3">
          <%= link_to "Date: #{log.date}", user_pet_log_path(@user, @pet, log) %>
          <table>
            <tr>
              <th><%= log.attr1 if log.attr1 %></th>
              <th><%= log.attr2 if log.attr2 %></th>
              <th><%= log.attr3 if log.attr3 %></th>
              <th><%= log.attr4 if log.attr4 %></th>
              <th><%= log.attr5 if log.attr5 %></th>
            </tr>
            <tr>
              <td><%= log.attr1_value if log.attr1 %></td>
              <td><%= log.attr2_value if log.attr2 %></td>
              <td><%= log.attr3_value if log.attr3 %></td>
              <td><%= log.attr4_value if log.attr4 %></td>
              <td><%= log.attr5_value if log.attr5 %></td>
            </tr>
          </table>
        </div>
      <% end %>
    <% else %>
      <p>No logs yet.</p>
    <% end %>
  </div>
  <%= form_with url: export_user_pet_logs_path(format: :pdf), method: :post, html: { onsubmit: "document.getElementById('loading-overlay').style.visibility = 'visible';" } do |f| %>
    <div class="date-range-selector">
      <%= f.label :start_date, "Start Date:" %>
      <%= f.date_field :start_date, id: "start-date", required: true %>
    </div>
    <div class="date-range-selector">
      <%= f.label :end_date, "End Date:" %>
      <%= f.date_field :end_date, id: "end-date", required: true, max: Date.today %>
    </div>
    <div class="language-selector">
      <%= f.label :language, "Language:" %>
      <%= f.select :language, ["English", "日本語", "Português"]%>
    </div>
    <%= f.submit "Export Data"%>
  <% end %>
</div>
<style>
  .card-logs{
    border-radius: 8px;
    border: 2px solid black;
    width: 70%;
  }

  .logs-all{
    width: 100%;
  }
</style>
