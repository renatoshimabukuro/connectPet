<div class="container">
  <h1>Pets#index</h1>
  <p>Find me in app/views/pets/index.html.erb</p>
  <% @pets.each do |pet| %>
    <h2><%= link_to pet.name, user_pet_path(current_user, pet) %></h2>
  <% end %>
</div>
